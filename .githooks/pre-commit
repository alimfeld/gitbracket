#!/bin/sh
# Run the validator + test suite before every commit: the hook is the fast
# local gate, gb.js publish re-runs validate before it ships, so a bypassed
# hook can't ship.
set -e
cd "$(git rev-parse --show-toplevel)"
node gb.js validate || exit 1
exec node --test
